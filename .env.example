# ── Base de datos MongoDB (para referencia histórica, ya no se usa en v0.2.0+)
# MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/indhira

# ── JWT
JWT_SECRET=cambiar_este_valor_por_uno_seguro_de_al_menos_32_caracteres
JWT_EXPIRES_IN=8h

# ── Servidor
PORT=3001
NODE_ENV=development

# ── (v0.2.0+) PostgreSQL
DB_HOST=postgres
DB_PORT=5432
DB_NAME=indhira_db
DB_USER=postgres
DB_PASSWORD=postgres
