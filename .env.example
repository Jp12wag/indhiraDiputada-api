# ── JWT ────────────────────────────────────────────────────
JWT_SECRET=cambiar_este_valor_por_uno_seguro_de_al_menos_32_caracteres
JWT_EXPIRES_IN=8h

# ── Servidor ────────────────────────────────────────────────
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# ── PostgreSQL (v0.2.0+) ────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=indhira_db
DB_USER=indhira
DB_PASS=indhira_pass

# ── MongoDB (solo si todavía usas v0.1.0 local) ─────────────
# MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/indhira
